package demo;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.edge.EdgeDriver;

public class Launch {
	public static void main(String[] args) {
		//ChromeDriver cd=new ChromeDriver();
		//EdgeDriver ed=new EdgeDriver(); // object of browser is created and stored in the same pace
		WebDriver wd=new ChromeDriver();  // object of chrome driver is created and stored in the nearest interface (Web driver).
	}
}
